# Luciferase-sequence-clustering
Analysis of luciferase protein sequences using k-mer feature extraction, dimensionality reduction (UMAP/PCA), and unsupervised clustering methods including hierarchical clustering and k-means.
